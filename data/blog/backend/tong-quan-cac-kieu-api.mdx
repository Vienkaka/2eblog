---
title: 'Tổng quan về các Kiểu API (API Styles) - REST, JSON API, SOAP, gRPC, GraphQL'
thumnail: 'https://fastly.picsum.photos/id/553/4288/2848.jpg?hmac=GFwL-rqthwP4zWlMpIz4JfwsyEKeXh2MTBhQoV7hPT8'
date: '2025-10-27T10:00:00Z'
lastmod: '2025-10-27T10:00:00Z'
tags: ['api', 'rest', 'graphql', 'grpc', 'json-api', 'soap', 'backend', 'web-development']
draft: false
summary: 'Hướng dẫn chi tiết về 5 kiểu API phổ biến: REST, JSON API, SOAP, gRPC, và GraphQL. Tìm hiểu đặc điểm, ưu nhược điểm, và cách chọn API phù hợp cho dự án của bạn.'
images:
  [
    'https://fastly.picsum.photos/id/553/4288/2848.jpg?hmac=GFwL-rqthwP4zWlMpIz4JfwsyEKeXh2MTBhQoV7hPT8',
  ]
authors: ['default']
layout: PostLayout
canonicalUrl: https://gaoden.vercel.app/blog/backend/tong-quan-cac-kieu-api
---

## Giới thiệu

API (Application Programming Interface) là cầu nối cho phép các ứng dụng giao tiếp và trao đổi dữ liệu với nhau. Tùy theo yêu cầu về hiệu năng, tính linh hoạt, hoặc khả năng tương thích, các API có thể được thiết kế theo nhiều phong cách khác nhau — gọi là **API Styles**.

Trong bài này, chúng ta sẽ tìm hiểu **5 kiểu phổ biến nhất** hiện nay:

1. **REST** (Representational State Transfer)
2. **JSON API**
3. **SOAP** (Simple Object Access Protocol)
4. **gRPC** (Google Remote Procedure Call)
5. **GraphQL**

Mỗi kiểu có những ưu điểm, nhược điểm, và các use case riêng. Hãy cùng khám phá!

---

## 1️⃣ REST (Representational State Transfer)

### 🌐 Tổng quan

**REST** là phong cách thiết kế API phổ biến nhất trên web hiện nay. Được giới thiệu bởi **Roy Fielding** vào năm 2000, REST sử dụng giao thức HTTP và tuân theo các nguyên tắc về **stateless**, **client-server**, và **cacheable**.

### ⚙️ Cách hoạt động

Mỗi resource (tài nguyên) được biểu diễn bằng một URL duy nhất và được truy cập thông qua các HTTP methods:

| Method    | Hành động   | Ví dụ        |
| --------- | ----------- | ------------ |
| GET       | Lấy dữ liệu | `/users`     |
| POST      | Tạo mới     | `/users`     |
| PUT/PATCH | Cập nhật    | `/users/:id` |
| DELETE    | Xóa         | `/users/:id` |

Dữ liệu thường được truyền ở dạng **JSON**, đôi khi là **XML**.

#### Ví dụ REST API

```javascript
// GET /users/1
{
  "id": 1,
  "name": "John Doe",
  "email": "john@example.com"
}

// POST /users
{
  "name": "Jane Smith",
  "email": "jane@example.com"
}

// PUT /users/1
{
  "id": 1,
  "name": "John Updated",
  "email": "john.updated@example.com"
}

// DELETE /users/1
// Response: 204 No Content
```

### ✅ Ưu điểm

- ✅ Dễ hiểu, dễ triển khai, dễ test
- ✅ Tận dụng tối đa hạ tầng HTTP (status codes, headers, caching)
- ✅ Tách biệt rõ ràng giữa frontend–backend (stateless)
- ✅ Dễ tích hợp với các công cụ và ngôn ngữ lập trình
- ✅ Hỗ trợ caching HTTP tự nhiên

### ❌ Nhược điểm

- ❌ Có thể dư thừa dữ liệu khi cần nhiều resource cùng lúc (**over-fetching**)
- ❌ Cần gọi nhiều endpoint để lấy dữ liệu liên quan (**under-fetching**)
- ❌ Không tối ưu cho các truy vấn phức tạp hoặc quan hệ lồng nhau
- ❌ Cần versioning API khi thay đổi cấu trúc

### 💼 Ứng dụng

- API của hầu hết các ứng dụng web như **Twitter, GitHub, Stripe** đều theo REST
- Phù hợp cho hệ thống CRUD đơn giản, microservices, hoặc mobile backend
- Lý tưởng cho dự án cần caching tốt và hiệu suất ổn định

---

## 2️⃣ JSON API

### 🌐 Tổng quan

**JSON API** là một specification (chuẩn định dạng) dựa trên REST, được xây dựng để **chuẩn hóa cách trao đổi dữ liệu JSON** giữa client và server.

### ⚙️ Đặc điểm

Khác với REST truyền thống, JSON API định nghĩa **chặt chẽ cấu trúc response và request**. Tất cả dữ liệu đều nằm trong key `"data"`, còn lỗi thì trong `"errors"`.

#### Ví dụ JSON API Response

```json
{
  "data": {
    "type": "articles",
    "id": "1",
    "attributes": {
      "title": "Understanding JSON API",
      "body": "The shortest introduction to JSON:API..."
    },
    "relationships": {
      "author": {
        "data": {
          "id": "9",
          "type": "people"
        }
      }
    }
  },
  "included": [
    {
      "type": "people",
      "id": "9",
      "attributes": {
        "name": "Jane Doe",
        "email": "jane@example.com"
      }
    }
  ]
}
```

#### Ví dụ JSON API Request

```json
// POST /articles
{
  "data": {
    "type": "articles",
    "attributes": {
      "title": "New Article",
      "body": "Article body here..."
    },
    "relationships": {
      "author": {
        "data": {
          "type": "people",
          "id": "9"
        }
      }
    }
  }
}
```

### ✅ Ưu điểm

- ✅ Chuẩn hóa cách giao tiếp giúp giảm boilerplate code
- ✅ Tự động hỗ trợ pagination, filtering, sparse fieldsets, relationships
- ✅ Dễ parse và nhất quán giữa các service
- ✅ Hỗ trợ inclusion của related resources để giảm số request
- ✅ Chuẩn định dạng lỗi rõ ràng

### ❌ Nhược điểm

- ❌ Cấu trúc JSON phức tạp, khó đọc hơn REST thuần
- ❌ Không phải client nào cũng hỗ trợ spec JSON API
- ❌ Không linh hoạt khi muốn custom response
- ❌ Có thể dẫn đến dư thừa dữ liệu khi included quá nhiều

### 💼 Ứng dụng

- Phù hợp cho hệ thống lớn cần consistency cao giữa nhiều team
- Dùng phổ biến trong các API của **Ember.js, Laravel JSON:API, Rails JSONAPI::Resources**
- Lý tưởng cho các dự án enterprise cần standardization

---

## 3️⃣ SOAP (Simple Object Access Protocol)

### 🌐 Tổng quan

**SOAP** là một giao thức (protocol) chuẩn dựa trên **XML**, được thiết kế cho việc trao đổi dữ liệu **an toàn và có cấu trúc** giữa các ứng dụng.

SOAP xuất hiện trước REST, thường được sử dụng trong môi trường doanh nghiệp hoặc hệ thống cần bảo mật cao.

### ⚙️ Cách hoạt động

SOAP message thường được đóng gói trong XML, có dạng như sau:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
  <soap:Body>
    <getUser>
      <userId>123</userId>
    </getUser>
  </soap:Body>
</soap:Envelope>
```

SOAP hoạt động qua HTTP, SMTP hoặc các giao thức khác.

#### Ví dụ SOAP Request/Response

```xml
<!-- Request -->
<soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
  <soap:Body>
    <createUser>
      <name>John Doe</name>
      <email>john@example.com</email>
    </createUser>
  </soap:Body>
</soap:Envelope>

<!-- Response -->
<soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
  <soap:Body>
    <createUserResponse>
      <userId>123</userId>
      <status>success</status>
    </createUserResponse>
  </soap:Body>
</soap:Envelope>
```

### ✅ Ưu điểm

- ✅ Chuẩn hóa cao, hỗ trợ **WSDL** (Web Services Description Language) giúp mô tả API tự động
- ✅ Hỗ trợ transaction, authentication, encryption mạnh mẽ
- ✅ Phù hợp với hệ thống có yêu cầu về bảo mật và độ tin cậy
- ✅ Message có cấu trúc rõ ràng, dễ validate
- ✅ Hỗ trợ multiple transport protocols

### ❌ Nhược điểm

- ❌ Cấu trúc XML cồng kềnh, khó đọc
- ❌ Hiệu suất kém hơn JSON-based APIs (XML lớn hơn JSON)
- ❌ Cấu hình phức tạp, không linh hoạt như REST hoặc GraphQL
- ❌ Debug khó hơn vì payload là XML
- ❌ Ít được sử dụng trong các ứng dụng web hiện đại

### 💼 Ứng dụng

- Phổ biến trong hệ thống **ngân hàng, bảo hiểm, chính phủ**, hoặc doanh nghiệp lớn
- Ví dụ: Hệ thống thanh toán hoặc CRM của các công ty truyền thống
- Vẫn được sử dụng trong một số legacy systems

---

## 4️⃣ gRPC (Google Remote Procedure Call)

### 🌐 Tổng quan

**gRPC** là một framework **RPC** (Remote Procedure Call) hiện đại do **Google** phát triển.
Nó sử dụng **HTTP/2** và **Protocol Buffers** (protobuf) để truyền dữ liệu nhị phân, cực nhanh và hiệu quả.

### ⚙️ Cách hoạt động

Thay vì gửi request bằng JSON, gRPC định nghĩa các service và message trong file `.proto`:

```protobuf
syntax = "proto3";

package user;

service UserService {
  rpc GetUser(GetUserRequest) returns (UserResponse);
  rpc ListUsers(ListUsersRequest) returns (stream UserResponse);
  rpc CreateUser(CreateUserRequest) returns (UserResponse);
}

message GetUserRequest {
  string id = 1;
}

message ListUsersRequest {
  int32 page = 1;
  int32 page_size = 2;
}

message UserResponse {
  string id = 1;
  string name = 2;
  string email = 3;
  int64 created_at = 4;
}

message CreateUserRequest {
  string name = 1;
  string email = 2;
}
```

Sau đó, gRPC compiler tự động tạo code client và server trong nhiều ngôn ngữ (Node.js, Go, Java, Python...).

#### Ví dụ gRPC Client (Node.js)

```javascript
const grpc = require('@grpc/grpc-js')
const loader = require('@grpc/proto-loader')

const packageDefinition = loader.loadSync('user.proto', {
  keepCase: true,
  longs: String,
  enums: String,
  defaults: true,
  oneofs: true,
})

const userProto = grpc.loadPackageDefinition(packageDefinition)
const client = new userProto.user.UserService('localhost:50051', grpc.credentials.createInsecure())

// Call RPC method
client.GetUser({ id: '123' }, (err, response) => {
  if (err) throw err
  console.log(response)
})

// Streaming
const call = client.ListUsers({ page: 1, page_size: 10 })
call.on('data', (user) => {
  console.log('User:', user)
})
```

### ✅ Ưu điểm

- ✅ **Nhanh và hiệu quả** hơn REST (do dùng binary protobuf)
- ✅ Hỗ trợ **streaming** (client-stream, server-stream, bidirectional)
- ✅ Hỗ trợ **đa ngôn ngữ** mạnh mẽ (code generation tự động)
- ✅ Giao tiếp **nội bộ microservices cực kỳ hiệu quả**
- ✅ Sử dụng HTTP/2 cho multiplexing và latency thấp

### ❌ Nhược điểm

- ❌ Debug khó hơn (vì dữ liệu không phải JSON)
- ❌ Không phù hợp với trình duyệt (cần gRPC-Web gateway)
- ❌ Học và setup phức tạp hơn REST
- ❌ Ít library và tool hỗ trợ so với REST

### 💼 Ứng dụng

- Dùng phổ biến trong **hệ thống microservices**, đặc biệt nội bộ backend
- **Netflix, Google, và Kubernetes API** đều sử dụng gRPC cho tốc độ cao
- Phù hợp cho real-time communication, streaming data, high-performance systems

---

## 5️⃣ GraphQL

### 🌐 Tổng quan

**GraphQL** được phát triển bởi **Facebook** năm 2012, là **ngôn ngữ truy vấn dữ liệu** (query language) cho API.

Khác với REST — nơi client gọi nhiều endpoint — GraphQL cho phép client chỉ cần **1 endpoint duy nhất** để yêu cầu đúng dữ liệu mình cần.

### ⚙️ Cách hoạt động

Client gửi một query mô tả dữ liệu muốn lấy:

```graphql
query {
  user(id: "1") {
    name
    email
    posts {
      id
      title
      createdAt
    }
  }
}
```

Server phản hồi:

```json
{
  "data": {
    "user": {
      "name": "John Doe",
      "email": "john@example.com",
      "posts": [
        {
          "id": "1",
          "title": "Intro to GraphQL",
          "createdAt": "2025-10-30T10:00:00Z"
        }
      ]
    }
  }
}
```

#### Ví dụ GraphQL Mutation

```graphql
mutation {
  createUser(name: "Jane Smith", email: "jane@example.com") {
    id
    name
    email
    createdAt
  }
}
```

### ✅ Ưu điểm

- ✅ **Lấy đúng dữ liệu cần** (không over-fetch hoặc under-fetch)
- ✅ **Giảm số lượng request** so với REST
- ✅ **Schema rõ ràng**, tự sinh documentation
- ✅ **Tối ưu cho frontend**, đặc biệt khi UI phức tạp
- ✅ **Mạnh mẽ cho rapid development** (frontend có thể thay đổi query nhanh)

### ❌ Nhược điểm

- ❌ **Server-side logic phức tạp** hơn (cần resolver optimization)
- ❌ **Không tận dụng được caching HTTP** chuẩn
- ❌ **Cần quản lý performance** (query nặng có thể chậm)
- ❌ Query validation và error handling phức tạp
- ❌ Learning curve cao cho developer mới

### 💼 Ứng dụng

- Dùng phổ biến trong **frontend hiện đại** (React, Next.js, Apollo, Relay)
- Phù hợp với hệ thống cần **linh hoạt dữ liệu**, ví dụ: dashboard, mobile app
- Lý tưởng cho các công ty muốn **decoupling backend–frontend**

---

## ⚖️ So sánh Tổng quan

| Tiêu chí               | REST     | JSON API        | SOAP             | gRPC             | GraphQL              |
| ---------------------- | -------- | --------------- | ---------------- | ---------------- | -------------------- |
| **Giao thức**          | HTTP     | HTTP            | HTTP, SMTP       | HTTP/2           | HTTP                 |
| **Định dạng dữ liệu**  | JSON/XML | JSON            | XML              | Protobuf         | JSON                 |
| **Tốc độ**             | ⭐⭐⭐   | ⭐⭐⭐          | ⭐               | ⭐⭐⭐⭐⭐       | ⭐⭐⭐               |
| **Linh hoạt dữ liệu**  | ⭐⭐     | ⭐              | ⭐               | ⭐⭐             | ⭐⭐⭐⭐⭐           |
| **Hỗ trợ trình duyệt** | ✅       | ✅              | ✅               | ⚠️ (qua gateway) | ✅                   |
| **Dễ debug**           | ✅       | ⚠️              | ❌               | ❌               | ✅                   |
| **Caching HTTP**       | ✅       | ✅              | ⚠️               | ⚠️               | ❌                   |
| **Phù hợp cho**        | Web API  | Enterprise REST | Doanh nghiệp lớn | Microservices    | Frontend UI phức tạp |

---

## 🧭 Khi nào sử dụng từng API style?

### Chọn REST khi:

- ✅ Xây dựng public API đơn giản, CRUD-heavy
- ✅ Cần caching HTTP tốt
- ✅ Team không lớn, không cần standardization quá chặt
- ✅ Ưu tiên tốc độ phát triển và dễ hiểu

### Chọn JSON API khi:

- ✅ Dự án enterprise với nhiều team
- ✅ Cần consistency cao giữa các endpoint
- ✅ Muốn giảm boilerplate code trên client
- ✅ API phục vụ nhiều client khác nhau

### Chọn SOAP khi:

- ✅ Hệ thống cần bảo mật rất cao (ngân hàng, chính phủ)
- ✅ Yêu cầu transaction chặt chẽ
- ✅ Là legacy system hoặc kế thừa từ SOAP cũ
- ⚠️ Lưu ý: Ít được sử dụng trong các dự án mới

### Chọn gRPC khi:

- ✅ Giao tiếp **nội bộ giữa microservices**
- ✅ Cần **tốc độ cực cao** (latency thấp)
- ✅ Có streaming data hoặc real-time communication
- ✅ Hệ thống phân tán có nhiều ngôn ngữ khác nhau

### Chọn GraphQL khi:

- ✅ Frontend yêu cầu **linh hoạt dữ liệu**
- ✅ Có UI **phức tạp với yêu cầu dữ liệu đa dạng**
- ✅ Cần **giảm network requests**
- ✅ Team frontend muốn **tự kiểm soát query**

---

## 🏗️ Kiến trúc Hybrid (Kết hợp nhiều API)

Trong thực tế, **không cần chỉ chọn một kiểu API duy nhất**. Nhiều hệ thống lớn kết hợp:

```
┌─────────────────┐
│   Frontend UI   │
│   (React/Vue)   │
└────────┬────────┘
         │
    GraphQL / REST
         │
┌────────▼────────────────┐
│  Public-facing API      │
│  (REST hoặc GraphQL)    │
└────────┬─────────────────┘
         │
    ┌────┴──────────────────┐
    │                       │
  gRPC                    REST
    │                       │
┌───▼────────┐    ┌────────▼──────┐
│  Service A │    │  Service B    │
│ (User Mgmt)│    │(Post Service) │
└────────────┘    └───────────────┘
```

**Ví dụ:**

- **REST API** cho public developers
- **gRPC** cho giao tiếp giữa backend services
- **GraphQL** cho mobile app hoặc web dashboard

---

## 🎯 Kết luận

Mỗi API style có thế mạnh riêng:

| Kiểu API     | Điểm mạnh                        | Tốt nhất cho              |
| ------------ | -------------------------------- | ------------------------- |
| **REST**     | Đơn giản, linh hoạt, caching tốt | Public APIs, CRUD systems |
| **JSON API** | Chuẩn hóa cao, consistency       | Enterprise systems        |
| **SOAP**     | Bảo mật mạnh, transaction        | Legacy enterprise systems |
| **gRPC**     | Tốc độ siêu cao, streaming       | Microservices, real-time  |
| **GraphQL**  | Linh hoạt dữ liệu, no over-fetch | Modern frontend UIs       |

**Nhớ rằng:**

- ❌ **Không có lựa chọn "tốt nhất"** — chỉ có lựa chọn **phù hợp** với nhu cầu
- ✅ Trong thực tế, nhiều hệ thống **kết hợp nhiều kiểu API** khác nhau
- ✅ **REST cho public API**, **gRPC cho nội bộ backend**, và **GraphQL cho frontend UI**

Hãy hiểu rõ từng kiểu API, rồi lựa chọn (hoặc kết hợp) cái phù hợp nhất với dự án của bạn!

---

## 📚 Tài liệu Tham khảo

- [REST Architectural Constraints](https://www.restfulapi.net/)
- [JSON:API Specification](https://jsonapi.org/)
- [SOAP Tutorial](https://www.w3schools.com/xml/xml_soap.asp)
- [gRPC Official Documentation](https://grpc.io/docs/)
- [GraphQL Official Documentation](https://graphql.org/learn/)
- [API Design Best Practices](https://swagger.io/blog/api-design/)
- [Microservices Communication Patterns](https://microservices.io/patterns/communication-style/rpc.html)
